/*
Il y a deux modes de fonctionnement possible de gitStories:
1. Utiliser un seul repo, toujours le même. 
   Pour cela, il suffit de configurer la constante `defaultRepoUrl`.
   Si on ne spécifie pas de tags, toutes les issues sont prises
2. Avoir plusieurs repos que l'on veut suivre. 
   Pour cela, il faut configurer le tableau `repos`.
   Le nom qui apparaîtra dans la liste déroulante est le nom du repo, sauf si un alias est précisé. 
   Il est également possible de spécifier la liste des tags à prendre comme user story.
*/

const defaultRepo = { url: "https://github.com/user/repo" };
const repos = [
  { url: "https://github.com/joe/project1" },
  { alias: "Jack", url: "https://github.com/jack/myLittleProject" },
  { url: "https://github.com/bill/myApp" },
  { url: "https://github.com/ETML-INF/escape" , tags: ["UserStory", "TechnicalStory"]}
];
const filterTags = []
const defaultAuthToken = "ghp_o0i..."; // Make sure the token has full private repo privileges
